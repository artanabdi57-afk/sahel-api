import React, { useEffect, useState } from "react";
import { Wallet, Receipt } from "lucide-react";
import { apiRequest, formatMoney } from "../../lib/api";
import { EmptyState, ErrorState, LoadingState } from "../../components/AsyncState";

export default function GymPayments() {
  const [members, setMembers] = useState([]), [payments, setPayments] = useState([]);
  const [form, setForm] = useState({ member_id: "", amount: "", payment_method: "cash", paid_for_month: "", extend_paid_until: "" });
  const [status, setStatus] = useState({ loading: true, saving: false, error: "" });
  async function load() { setStatus((s) => ({ ...s, loading: true, error: "" })); try { const [m, p] = await Promise.all([apiRequest("/gym/members"), apiRequest("/gym/payments")]); setMembers(m.data || []); setPayments(p.data || []); } catch (e) { setStatus((s) => ({ ...s, error: e.message })); } finally { setStatus((s) => ({ ...s, loading: false })); } }
  useEffect(() => { load(); }, []);
  const set = (k, v) => setForm((f) => ({ ...f, [k]: v }));
  async function submit(e) { e.preventDefault(); setStatus((s) => ({ ...s, saving: true, error: "" })); try { await apiRequest("/gym/payments", { method: "POST", body: JSON.stringify({ ...form, amount: Number(form.amount), paid_for_month: form.paid_for_month || undefined, extend_paid_until: form.extend_paid_until || undefined }) }); setForm({ member_id: "", amount: "", payment_method: "cash", paid_for_month: "", extend_paid_until: "" }); await load(); } catch (e) { setStatus((s) => ({ ...s, error: e.message })); } finally { setStatus((s) => ({ ...s, saving: false })); } }
  if (status.loading) return <LoadingState />;
  const total = payments.reduce((sum, p) => sum + Number(p.amount || 0), 0);
  return <div className="space-y-5 motion-card"><div className="rounded-2xl border border-blue-100 bg-gradient-to-r from-white via-blue-50/50 to-indigo-50 p-5"><p className="text-xs font-bold uppercase tracking-[0.18em] text-blue-600">Gym revenue</p><div className="mt-1 flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-2xl font-black">Payments</h2><p className="text-sm text-slate-500">Record membership payments and keep paid-until dates up to date.</p></div><div className="rounded-xl bg-white px-4 py-3 shadow-sm ring-1 ring-blue-100"><p className="text-xs font-semibold text-slate-500">History total</p><p className="text-xl font-black text-blue-600">{formatMoney(total)}</p></div></div></div>{status.error && <ErrorState message={status.error} />}<div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form onSubmit={submit} className="panel h-fit p-5 transition duration-300 hover:-translate-y-0.5 hover:shadow-md"><h2 className="mb-4 flex items-center gap-2 text-lg font-black"><Wallet className="h-5 w-5 text-blue-600" /> Record a payment</h2><div className="space-y-3"><select className="field" value={form.member_id} onChange={(e) => set("member_id", e.target.value)} required><option value="">Choose member…</option>{members.map((m) => <option key={m.id} value={m.id}>{m.name}</option>)}</select><input className="field" type="number" min="0.01" step="0.01" placeholder="Amount" value={form.amount} onChange={(e) => set("amount", e.target.value)} required /><select className="field" value={form.payment_method} onChange={(e) => set("payment_method", e.target.value)}><option value="cash">Cash</option><option value="bank">Bank</option><option value="mobile_money">Mobile money</option></select><input className="field" type="date" value={form.paid_for_month} onChange={(e) => set("paid_for_month", e.target.value)} /><div><label className="mb-1 block text-xs font-semibold text-slate-500">Extend paid-until</label><input className="field" type="date" value={form.extend_paid_until} onChange={(e) => set("extend_paid_until", e.target.value)} /></div></div><button className="btn-primary mt-4 w-full" disabled={status.saving}>{status.saving ? "Saving…" : "Record payment"}</button></form><div className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 p-4"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-600">Transactions</p><h2 className="font-black">Payment history</h2></div><Receipt className="h-5 w-5 text-slate-400" /></div>{payments.length === 0 ? <div className="flex min-h-[300px] items-center justify-center p-8"><EmptyState title="No payments yet" description="Record a membership payment to see the transaction history here." /></div> : <div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Member</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Paid</th><th className="px-4 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{payments.map((p) => <tr key={p.id} className="transition hover:bg-blue-50/40"><td className="px-4 py-3 font-bold">{p.gym_members?.name || "-"}</td><td className="px-4 py-3 capitalize">{p.payment_method}</td><td className="px-4 py-3">{p.paid_at ? new Date(p.paid_at).toLocaleDateString() : "-"}</td><td className="px-4 py-3 text-right font-black">{formatMoney(p.amount)}</td></tr>)}</tbody></table></div>}</div></div></div>;
}
