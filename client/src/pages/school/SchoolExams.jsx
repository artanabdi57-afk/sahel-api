import React, { useEffect, useMemo, useState } from "react";
import { ArrowLeft, BookOpen, CheckCircle2, History, Save, Settings2, Sparkles } from "lucide-react";
import { apiRequest } from "../../lib/api";
import { EmptyState, ErrorState, LoadingState } from "../../components/AsyncState";

const SUBJECTS = { primary: ["Arabic", "Science", "Math", "Technology", "Tarbiya", "Social Studies", "Somali", "English"], secondary: ["Arabic", "Tarbiya", "History", "Geography", "Chemistry", "Biology", "Technology", "Business", "Somali", "English", "Math"] };
const TERMS = [{ key: "term_1", label: "Term 1" }, { key: "term_2", label: "Term 2" }, { key: "term_3", label: "Term 3" }];
const ASSESSMENTS = ["Exam 1", "Exam 2", "Exam 3", "Exam 4"];
const DEFAULT_LIMITS = [20, 20, 20, 20];
const defaultAcademicYear = () => { const y = new Date().getFullYear(); return new Date().getMonth() >= 7 ? `${y}-${y + 1}` : `${y - 1}-${y}`; };
const termLabel = (term) => TERMS.find((x) => x.key === term)?.label || term;
const keyFor = (studentId, subject, attempt) => `${studentId}:${subject}:${attempt}`;
const safeLimit = (value, fallback = 20) => { const n = Number(value); return Number.isFinite(n) && n >= 1 && n <= 100 ? n : fallback; };
const limitsFromExam = (exam) => [safeLimit(exam?.assessment_one_max), safeLimit(exam?.assessment_two_max), safeLimit(exam?.assessment_three_max), safeLimit(exam?.assessment_four_max)];

function HistoryPanel({ studentId, onClose }) {
  const [state, setState] = useState({ loading: true, error: "", student: null, data: [] });
  useEffect(() => { apiRequest(`/school/students/${studentId}/exam-history`).then((r) => setState({ loading: false, error: "", student: r.student, data: r.data || [] })).catch((e) => setState({ loading: false, error: e.message, student: null, data: [] })); }, [studentId]);
  if (state.loading) return <LoadingState />; if (state.error) return <ErrorState message={state.error} />;
  const grouped = state.data.reduce((acc, item) => { const key = `${item.school_exams?.academic_year || "Unknown year"} · ${termLabel(item.school_exams?.term)}`; (acc[key] ||= []).push(item); return acc; }, {});
  return <div className="motion-pop fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-4 backdrop-blur-sm" onMouseDown={(e) => e.target === e.currentTarget && onClose()}><div className="max-h-[90vh] w-full max-w-6xl overflow-hidden rounded-3xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-orange-600">Student academic history</p><h2 className="mt-1 text-xl font-black">{state.student?.name}</h2><p className="text-sm text-slate-500">School ID {state.student?.registration_no} · {state.student?.school_classes?.name || "No class"}</p></div><button className="btn-secondary" onClick={onClose}>Close</button></div><div className="max-h-[72vh] overflow-auto p-5">{!state.data.length ? <EmptyState title="No past results" description="This student does not have saved examination results yet." /> : <div className="space-y-5">{Object.entries(grouped).map(([group, items]) => <div key={group} className="overflow-hidden rounded-2xl border border-slate-200"><div className="bg-orange-50 px-4 py-3 font-black text-orange-800">{group}</div><table className="min-w-full text-left text-sm"><thead className="bg-slate-50"><tr className="border-b text-xs uppercase text-slate-500"><th className="px-4 py-3">Subject</th><th className="px-3 py-3">Exam 1</th><th className="px-3 py-3">Exam 2</th><th className="px-3 py-3">Exam 3</th><th className="px-3 py-3">Exam 4</th><th className="px-3 py-3">Total</th></tr></thead><tbody className="divide-y">{items.map((item, i) => { const limits = [safeLimit(item.school_exams?.assessment_one_max), safeLimit(item.school_exams?.assessment_two_max), safeLimit(item.school_exams?.assessment_three_max), safeLimit(item.school_exams?.assessment_four_max)]; return <tr key={`${item.subject}-${i}`}><td className="px-4 py-3 font-semibold">{item.subject}</td><td className="px-3 py-3">{item.attempt_one ?? 0}/{limits[0]}</td><td className="px-3 py-3">{item.attempt_two ?? 0}/{limits[1]}</td><td className="px-3 py-3">{item.attempt_three ?? 0}/{limits[2]}</td><td className="px-3 py-3">{item.attempt_four ?? 0}/{limits[3]}</td><td className="px-3 py-3 font-black text-orange-700">{item.score ?? 0}/{limits.reduce((a, b) => a + b, 0)}</td></tr>; })}</tbody></table></div>)}</div>}</div></div></div>;
}

function ClassExamBook({ schoolClass, academicYear, onBack }) {
  const subjects = SUBJECTS[schoolClass.level === "secondary" ? "secondary" : "primary"];
  const [term, setTerm] = useState("term_1"); const [terms, setTerms] = useState([]); const [students, setStudents] = useState([]); const [exam, setExam] = useState(null); const [limits, setLimits] = useState(DEFAULT_LIMITS); const [scores, setScores] = useState({}); const [historyStudent, setHistoryStudent] = useState(null); const [showSettings, setShowSettings] = useState(false); const [status, setStatus] = useState({ loading: true, saving: false, savingLimits: false, preparing: false, error: "", success: "" });
  const load = async (selectedTerm = term) => { setStatus((s) => ({ ...s, loading: true, error: "" })); try { const [er, sr] = await Promise.all([apiRequest(`/school/exams?class_id=${schoolClass.id}&academic_year=${encodeURIComponent(academicYear)}`), apiRequest(`/school/students?class_id=${schoolClass.id}`)]); const list = er.data || []; setTerms(list); setStudents(sr.data || []); const current = list.find((x) => x.term === selectedTerm) || null; setExam(current); setLimits(limitsFromExam(current)); if (current) { const rr = await apiRequest(`/school/exams/${current.id}/results`); const map = {}; (rr.data || []).forEach((r) => { map[keyFor(r.student_id, r.subject, 1)] = r.attempt_one ?? ""; map[keyFor(r.student_id, r.subject, 2)] = r.attempt_two ?? ""; map[keyFor(r.student_id, r.subject, 3)] = r.attempt_three ?? ""; map[keyFor(r.student_id, r.subject, 4)] = r.attempt_four ?? ""; }); setScores(map); } else setScores({}); } catch (e) { setStatus((s) => ({ ...s, error: e.message })); } finally { setStatus((s) => ({ ...s, loading: false })); } };
  useEffect(() => { load(term); }, [schoolClass.id, academicYear]);
  const prepareAllTerms = async () => { setStatus((s) => ({ ...s, preparing: true, error: "", success: "" })); try { for (const t of TERMS) await apiRequest("/school/exams", { method: "POST", body: JSON.stringify({ class_id: schoolClass.id, term: t.key, academic_year: academicYear, assessment_one_max: 20, assessment_two_max: 20, assessment_three_max: 20, assessment_four_max: 20 }) }); await load(term); setStatus((s) => ({ ...s, preparing: false, success: "All 3 terms are ready. Each exam starts out of 20, and you can customize each limit." })); } catch (e) { setStatus((s) => ({ ...s, preparing: false, error: e.message })); } };
  const setScore = (studentId, subject, attempt, value) => { const max = limits[attempt - 1]; const raw = value === "" ? "" : Number(value); const next = value === "" ? "" : Number.isFinite(raw) ? Math.min(Math.max(raw, 0), max) : ""; setScores((s) => ({ ...s, [keyFor(studentId, subject, attempt)]: next })); };
  const setLimit = (attempt, value) => { const index = attempt - 1; const nextLimit = Math.min(100, Math.max(1, Number(value) || 1)); setLimits((current) => current.map((v, i) => i === index ? nextLimit : v)); setScores((current) => { const next = { ...current }; students.forEach((student) => subjects.forEach((subject) => { const key = keyFor(student.id, subject, attempt); if (next[key] !== "" && next[key] !== undefined) next[key] = Math.min(Number(next[key]) || 0, nextLimit); })); return next; }); };
  const saveLimits = async () => { if (!exam) return; setStatus((s) => ({ ...s, savingLimits: true, error: "", success: "" })); try { const r = await apiRequest("/school/exams", { method: "POST", body: JSON.stringify({ class_id: schoolClass.id, term, academic_year: academicYear, assessment_one_max: limits[0], assessment_two_max: limits[1], assessment_three_max: limits[2], assessment_four_max: limits[3] }) }); setExam(r.data); setLimits(limitsFromExam(r.data)); setShowSettings(false); setStatus((s) => ({ ...s, savingLimits: false, success: `Exam limits saved: ${limits.join(" + ")} = ${limits.reduce((a, b) => a + b, 0)} total marks.` })); } catch (e) { setStatus((s) => ({ ...s, savingLimits: false, error: e.message })); } };
  const totalFor = (studentId, subject) => [1,2,3,4].reduce((sum, attempt) => sum + (Number(scores[keyFor(studentId, subject, attempt)]) || 0), 0); const totalMax = limits.reduce((sum, value) => sum + value, 0);
  const save = async () => { if (!exam) return; setStatus((s) => ({ ...s, saving: true, error: "", success: "" })); try { const results = []; students.forEach((student) => subjects.forEach((subject) => { const values = [1,2,3,4].map((attempt) => scores[keyFor(student.id, subject, attempt)]); if (values.some((v) => v !== "" && v !== undefined && v !== null)) results.push({ student_id: student.id, subject, attempt_one: values[0], attempt_two: values[1], attempt_three: values[2], attempt_four: values[3] }); })); await apiRequest(`/school/exams/${exam.id}/results`, { method: "POST", body: JSON.stringify({ results }) }); setStatus((s) => ({ ...s, saving: false, success: `${termLabel(term)} results saved. Maximum total is ${totalMax}.` })); await load(term); } catch (e) { setStatus((s) => ({ ...s, saving: false, error: e.message })); } };
  if (status.loading) return <LoadingState />;
  return <div className="space-y-5 motion-card"><div className="flex items-center gap-3"><button className="btn-secondary" onClick={onBack}><ArrowLeft className="h-4 w-4" /> Classes</button><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-orange-600">Examination book</p><h1 className="text-2xl font-black">{schoolClass.name}</h1></div></div><div className="rounded-3xl border border-orange-100 bg-gradient-to-r from-white via-orange-50 to-amber-50 p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-sm font-semibold text-slate-500">{schoolClass.level === "secondary" ? "Secondary · Dugsi Sare" : "Primary school"} · {academicYear}</p><h2 className="mt-1 text-xl font-black">Four-exam mark book</h2><p className="mt-1 text-sm text-slate-500">Set the maximum mark for each exam. Students can never enter a score higher than the number shown above.</p></div><div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={() => setShowSettings((v) => !v)}><Settings2 className="h-4 w-4" /> Customize exams</button><button className="btn-primary" onClick={prepareAllTerms} disabled={status.preparing}><Sparkles className="h-4 w-4" />{status.preparing ? "Preparing…" : "Prepare all 3 terms"}</button></div></div></div>
    {showSettings && exam && <div className="motion-pop rounded-2xl border border-orange-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-sm font-black">Customize {termLabel(term)}</p><p className="text-xs text-slate-500">Change an exam from 20 to 10, 15, 25, etc. The input boxes and total update automatically.</p></div><button className="btn-primary" onClick={saveLimits} disabled={status.savingLimits}>{status.savingLimits ? "Saving…" : "Save exam limits"}</button></div><div className="mt-4 grid grid-cols-2 gap-3 md:grid-cols-4">{ASSESSMENTS.map((label, i) => <label key={label} className="rounded-xl border border-slate-200 bg-slate-50 p-3"><span className="block text-xs font-bold text-slate-500">{label} maximum</span><div className="mt-2 flex items-center gap-2"><input type="number" min="1" max="100" step="1" className="field w-full text-center font-black" value={limits[i]} onChange={(e) => setLimit(i + 1, e.target.value)} /><span className="text-sm font-bold text-slate-500">marks</span></div></label>)}</div></div>}
    {status.error && <ErrorState message={status.error} />}{status.success && <div className="motion-pop rounded-xl border border-emerald-100 bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{status.success}</div>}
    <div className="grid gap-3 md:grid-cols-3">{TERMS.map((t) => { const exists = terms.find((x) => x.term === t.key); return <button key={t.key} onClick={() => { setTerm(t.key); load(t.key); }} className={`rounded-2xl border p-4 text-left transition hover:-translate-y-0.5 hover:shadow-md ${term === t.key ? "border-orange-300 bg-orange-50" : "border-slate-200 bg-white"}`}><div className="flex items-center justify-between"><span className="font-black">{t.label}</span>{exists ? <CheckCircle2 className="h-5 w-5 text-emerald-500" /> : <span className="text-xs text-slate-400">Not started</span>}</div><p className="mt-1 text-xs text-slate-500">{exists ? `Ready · ${limitsFromExam(exists).join(" + ")} marks` : "Not started"}</p></button>; })}</div>
    {!exam ? <div className="panel flex min-h-[300px] items-center justify-center p-8"><EmptyState title={`${termLabel(term)} has not been started`} description="Use Prepare all 3 terms to create the examination book for this class." /></div> : <div className="panel overflow-hidden"><div className="flex flex-col gap-3 border-b border-slate-100 p-4 lg:flex-row lg:items-center lg:justify-between"><div><h3 className="font-black">{termLabel(term)} · {students.length} students · {subjects.length} subjects</h3><p className="text-xs text-slate-500">Each exam has its own limit. Current maximum total: {totalMax} marks.</p></div><button className="btn-primary" onClick={save} disabled={status.saving}><Save className="h-4 w-4" />{status.saving ? "Saving…" : "Save results"}</button></div><div className="overflow-x-auto"><table className="min-w-[1700px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th rowSpan="2" className="sticky left-0 z-20 bg-slate-50 px-4 py-3">Student</th>{subjects.map((subject) => <th key={subject} colSpan="5" className="border-l border-slate-200 px-2 py-3 text-center text-orange-700">{subject}</th>)}<th rowSpan="2" className="px-4 py-3 text-center">History</th></tr><tr>{subjects.flatMap((subject) => [1,2,3,4].map((n) => <th key={`${subject}-${n}`} className="border-l border-slate-100 px-2 py-2 text-center"><div className="flex items-center justify-center gap-1"><span>E{n} /</span><input aria-label={`Exam ${n} maximum`} type="number" min="1" max="100" step="1" className="w-12 rounded-md border border-orange-200 bg-white px-1 py-1 text-center text-xs font-black text-orange-700 outline-none focus:border-orange-500" value={limits[n - 1]} onChange={(e) => setLimit(n, e.target.value)} /></div></th>))}</tr></thead><tbody className="divide-y divide-slate-100">{students.length ? students.map((student) => <tr key={student.id} className="transition hover:bg-orange-50/30"><td className="sticky left-0 z-10 bg-white px-4 py-3"><button className="text-left" onClick={() => setHistoryStudent(student.id)}><p className="font-black text-slate-900 hover:text-orange-600">{student.name}</p><p className="text-[11px] font-bold text-slate-400">ID {student.registration_no || "-"}</p></button></td>{subjects.map((subject) => <React.Fragment key={subject}>{[1,2,3,4].map((attempt) => <td key={`${subject}-${attempt}`} className="border-l border-slate-100 px-1 py-2"><input type="number" min="0" max={limits[attempt - 1]} step="1" className="field w-16 text-center" placeholder="0" value={scores[keyFor(student.id, subject, attempt)] ?? ""} onChange={(e) => setScore(student.id, subject, attempt, e.target.value)} /></td>)}<td className="border-l border-slate-200 bg-orange-50/50 px-2 py-2 text-center font-black text-orange-700">{totalFor(student.id, subject)}/{totalMax}</td></React.Fragment>)}<td className="px-4 py-3 text-center"><button className="inline-flex h-9 w-9 items-center justify-center rounded-lg border border-orange-100 text-orange-600 hover:bg-orange-50" title="View past examinations" onClick={() => setHistoryStudent(student.id)}><History className="h-4 w-4" /></button></td></tr>) : <tr><td colSpan={subjects.length * 5 + 2} className="p-10"><EmptyState title="No students in this class" description="Register students and assign them to this class first." /></td></tr>}</tbody></table></div><div className="border-t border-slate-100 bg-orange-50/60 p-4 text-sm font-semibold text-orange-900">Marking rule: <strong>{limits.join(" + ")} = {totalMax} marks</strong> for every subject. A score above an exam's maximum is automatically prevented.</div></div>}
    {historyStudent && <HistoryPanel studentId={historyStudent} onClose={() => setHistoryStudent(null)} />}</div>;
}

export default function SchoolExams() {
  const [classes, setClasses] = useState([]); const [academicYear, setAcademicYear] = useState(defaultAcademicYear()); const [selected, setSelected] = useState(null); const [status, setStatus] = useState({ loading: true, error: "" });
  const load = async () => { setStatus({ loading: true, error: "" }); try { const r = await apiRequest("/school/classes"); setClasses(r.data || []); setStatus({ loading: false, error: "" }); } catch (e) { setStatus({ loading: false, error: e.message }); } };
  useEffect(() => { load(); }, []);
  const summary = useMemo(() => ({ primary: classes.filter((c) => c.level !== "secondary").length, secondary: classes.filter((c) => c.level === "secondary").length }), [classes]);
  if (status.loading) return <LoadingState />; if (status.error) return <ErrorState message={status.error} />; if (selected) return <ClassExamBook schoolClass={selected} academicYear={academicYear} onBack={() => setSelected(null)} />;
  return <div className="space-y-5 motion-card"><div className="rounded-3xl border border-orange-100 bg-gradient-to-r from-white via-orange-50/70 to-amber-50 p-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-orange-600">Academic records</p><h1 className="mt-1 text-2xl font-black">Examination Book</h1><p className="mt-1 max-w-2xl text-sm text-slate-500">Choose a class, select a term, and enter four exams for every subject. Each exam maximum is customizable.</p></div><div className="flex items-center gap-2"><label className="text-xs font-bold text-slate-500">Academic year</label><input className="field w-36" value={academicYear} onChange={(e) => setAcademicYear(e.target.value)} placeholder="2026-2027" /></div></div><div className="mt-5 grid max-w-md grid-cols-2 gap-3"><div className="rounded-2xl border border-white bg-white/80 p-4"><p className="text-xs font-semibold text-slate-500">Primary classes</p><p className="mt-1 text-2xl font-black text-orange-700">{summary.primary}</p></div><div className="rounded-2xl border border-white bg-white/80 p-4"><p className="text-xs font-semibold text-slate-500">Secondary · Dugsi Sare</p><p className="mt-1 text-2xl font-black text-orange-700">{summary.secondary}</p></div></div></div>{!classes.length ? <div className="panel flex min-h-[360px] items-center justify-center"><EmptyState title="No classes yet" description="Create your Primary and Secondary classes first." /></div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{classes.map((schoolClass) => <button key={schoolClass.id} onClick={() => setSelected(schoolClass)} className="panel group p-5 text-left transition duration-300 hover:-translate-y-1 hover:border-orange-200 hover:shadow-lg"><div className="flex items-start justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-orange-100 text-orange-700"><BookOpen className="h-5 w-5" /></div><span className="rounded-full bg-orange-50 px-3 py-1 text-xs font-bold text-orange-700">{schoolClass.level === "secondary" ? "Dugsi Sare" : "Primary"}</span></div><h2 className="mt-4 text-lg font-black">{schoolClass.name}</h2><p className="mt-1 text-sm text-slate-500">Open this class examination book</p><div className="mt-4 flex items-center justify-between text-sm font-bold text-orange-700"><span>4 customizable exams</span><span>Open →</span></div></button>)}</div>}</div>;
}
