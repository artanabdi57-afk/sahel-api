import React,{useEffect,useState}from"react";import{ClipboardCheck,Trash2,Save}from"lucide-react";import{apiRequest}from"../../lib/api";import{EmptyState,ErrorState,LoadingState}from"../../components/AsyncState";function ResultsEntry({exam,onClose}){const[students,setStudents]=useState([]),[scores,setScores]=useState({});const[status,setStatus]=useState({loading:true,saving:false,error:"",success:""});useEffect(()=>{(async()=>{try{const[s,r]=await Promise.all([apiRequest(`/school/students${exam.class_id?`?class_id=${exam.class_id}`:""}`),apiRequest(`/school/exams/${exam.id}/results`)]);setStudents(s.data||[]);const map={};(r.data||[]).forEach(x=>{map[x.student_id]=x.score});setScores(map)}catch(e){setStatus(x=>({...x,error:e.message}))}finally{setStatus(x=>({...x,loading:false}))}})()},[exam.id,exam.class_id]);const save=async()=>{setStatus(s=>({...s,saving:true,error:"",success:""}));try{const results=Object.entries(scores).filter(([,v])=>v!==""&&v!=null).map(([student_id,score])=>({student_id,score:Number(score)}));await apiRequest(`/school/exams/${exam.id}/results`,{method:"POST",body:JSON.stringify({results})});setStatus(s=>({...s,saving:false,success:"Results saved."}))}catch(e){setStatus(s=>({...s,saving:false,error:e.message}))}};if(status.loading)return <LoadingState/>;return <div className="panel p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-bold">Results — {exam.name} <span className="font-medium text-slate-400">(out of {exam.max_score})</span></h2><button className="text-sm font-bold text-blue-600" onClick={onClose}>Close</button></div>{status.error&&<ErrorState message={status.error}/>} {status.success&&<div className="mb-4 rounded-lg bg-green-50 p-3 text-sm text-green-700">{status.success}</div>}{students.length===0?<EmptyState title="No students in this class" description="Assign students to this exam's class first."/>:<div className="space-y-2">{students.map(s=><div key={s.id} className="flex items-center justify-between rounded-lg border border-slate-100 px-3 py-2"><span className="font-semibold">{s.name}</span><input type="number" min="0" max={exam.max_score} className="field w-28 text-right" value={scores[s.id]??""} onChange={e=>setScores(sc=>({...sc,[s.id]:e.target.value}))}/></div>)}<button className="btn-primary mt-3" onClick={save} disabled={status.saving}><Save className="h-4 w-4"/>{status.saving?"Saving…":"Save results"}</button></div>}</div>}
export default function SchoolExams(){const[exams,setExams]=useState([]),[classes,setClasses]=useState([]),[openExam,setOpenExam]=useState(null);const[form,setForm]=useState({name:"",class_id:"",exam_date:"",max_score:"100"});const[status,setStatus]=useState({loading:true,saving:false,error:""});const load=async()=>{setStatus(s=>({...s,loading:true,error:""}));try{const[e,c]=await Promise.all([apiRequest("/school/exams"),apiRequest("/school/classes")]);setExams(e.data||[]);setClasses(c.data||[])}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,loading:false}))}};useEffect(()=>{load()},[]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async e=>{e.preventDefault();setStatus(s=>({...s,saving:true,error:""}));try{await apiRequest("/school/exams",{method:"POST",body:JSON.stringify({...form,max_score:Number(form.max_score)||100})});setForm({name:"",class_id:"",exam_date:"",max_score:"100"});await load()}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,saving:false}))}};const del=async id=>{if(!window.confirm("Remove this exam and all its results?"))return;try{await apiRequest(`/school/exams/${id}`,{method:"DELETE"});await load()}catch(e){setStatus(s=>({...s,error:e.message}))}};if(status.loading)return <LoadingState/>;if(openExam)return <ResultsEntry exam={openExam} onClose={()=>setOpenExam(null)}/>;return <div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form onSubmit={submit} className="panel h-fit p-4"><h2 className="mb-4 flex items-center gap-2 font-bold"><ClipboardCheck className="h-5 w-5 text-blue-600"/> Add Exam</h2>{status.error&&<ErrorState message={status.error}/>}<div className="space-y-3"><input className="field" placeholder="Exam name" value={form.name} onChange={e=>set("name",e.target.value)} required/><select className="field" value={form.class_id} onChange={e=>set("class_id",e.target.value)}><option value="">Choose class</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input className="field" type="date" value={form.exam_date} onChange={e=>set("exam_date",e.target.value)}/><input className="field" type="number" min="1" value={form.max_score} onChange={e=>set("max_score",e.target.value)}/></div><button className="btn-primary mt-4 w-full" disabled={status.saving}>{status.saving?"Saving…":"Add exam"}</button></form><div className="panel overflow-hidden">{exams.length===0?<div className="p-4"><EmptyState title="No exams yet" description="Add your first exam, then enter student scores."/></div>:<div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase"><tr><th className="px-4 py-3">Exam</th><th className="px-4 py-3">Class</th><th className="px-4 py-3">Date</th><th className="px-4 py-3 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{exams.map(ex=><tr key={ex.id}><td className="px-4 py-3 font-semibold">{ex.name}</td><td className="px-4 py-3">{ex.school_classes?.name||"-"}</td><td className="px-4 py-3">{ex.exam_date?new Date(ex.exam_date).toLocaleDateString():"-"}</td><td className="px-4 py-3 text-right"><div className="flex justify-end gap-2"><button type="button" className="btn-secondary" onClick={()=>setOpenExam(ex)}>Enter scores</button><button type="button" className="inline-flex h-9 w-9 items-center justify-center rounded-lg border border-red-100 text-red-600" onClick={()=>del(ex.id)}><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div>}</div></div>}
