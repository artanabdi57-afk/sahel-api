import React, { useEffect, useState } from "react";
import { Trash2, BookOpen } from "lucide-react";
import { apiRequest } from "../../lib/api";
import { EmptyState, ErrorState, LoadingState } from "../../components/AsyncState";
export default function SchoolClasses() { const [classes,setClasses]=useState([]),[teachers,setTeachers]=useState([]); const [form,setForm]=useState({name:"",grade:"",teacher_id:""}); const [status,setStatus]=useState({loading:true,saving:false,error:""}); const load=async()=>{setStatus(s=>({...s,loading:true,error:""}));try{const[c,t]=await Promise.all([apiRequest("/school/classes"),apiRequest("/school/teachers")]);setClasses(c.data||[]);setTeachers(t.data||[])}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,loading:false}))}};useEffect(()=>{load()},[]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async e=>{e.preventDefault();setStatus(s=>({...s,saving:true,error:""}));try{await apiRequest("/school/classes",{method:"POST",body:JSON.stringify(form)});setForm({name:"",grade:"",teacher_id:""});await load()}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,saving:false}))}};const del=async id=>{if(!window.confirm("Remove this class?"))return;try{await apiRequest(`/school/classes/${id}`,{method:"DELETE"});await load()}catch(e){setStatus(s=>({...s,error:e.message}))}};if(status.loading)return <LoadingState/>;return <div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form onSubmit={submit} className="panel h-fit p-4"><h2 className="mb-4 flex items-center gap-2 text-base font-bold"><BookOpen className="h-5 w-5 text-blue-600"/> Add Class</h2>{status.error&&<ErrorState message={status.error}/>}<div className="space-y-3"><input className="field" placeholder="Class name (e.g. Grade 5A)" value={form.name} onChange={e=>set("name",e.target.value)} required/><input className="field" placeholder="Grade / level" value={form.grade} onChange={e=>set("grade",e.target.value)}/><select className="field" value={form.teacher_id} onChange={e=>set("teacher_id",e.target.value)}><option value="">Assign teacher (optional)</option>{teachers.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></div><button className="btn-primary mt-4 w-full" disabled={status.saving}>{status.saving?"Saving…":"Add class"}</button></form><div className="panel overflow-hidden">{classes.length===0?<div className="p-4"><EmptyState title="No classes yet" description="Add your first class to start enrolling students."/></div>:<div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Class</th><th className="px-4 py-3">Grade</th><th className="px-4 py-3">Teacher</th><th className="px-4 py-3 text-right">Action</th></tr></thead><tbody className="divide-y divide-slate-100">{classes.map(c=><tr key={c.id}><td className="px-4 py-3 font-semibold">{c.name}</td><td className="px-4 py-3">{c.grade||"-"}</td><td className="px-4 py-3">{c.school_teachers?.name||"Unassigned"}</td><td className="px-4 py-3 text-right"><button type="button" className="inline-flex h-9 w-9 items-center justify-center rounded-lg border border-red-100 text-red-600" onClick={()=>del(c.id)}><Trash2 className="h-4 w-4"/></button></td></tr>)}</tbody></table></div>}</div></div>}
