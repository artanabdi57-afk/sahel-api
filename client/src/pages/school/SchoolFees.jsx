import React,{useEffect,useState}from"react";import{Wallet,AlertTriangle}from"lucide-react";import{apiRequest,formatMoney}from"../../lib/api";import{EmptyState,ErrorState,LoadingState}from"../../components/AsyncState";export default function SchoolFees(){const[students,setStudents]=useState([]),[payments,setPayments]=useState([]),[unpaid,setUnpaid]=useState([]);const[form,setForm]=useState({student_id:"",amount:"",payment_method:"cash",paid_for_month:""});const[status,setStatus]=useState({loading:true,saving:false,error:""});const load=async()=>{setStatus(s=>({...s,loading:true,error:""}));try{const[s,p,u]=await Promise.all([apiRequest("/school/students"),apiRequest("/school/fee-payments"),apiRequest("/school/fee-payments/unpaid")]);setStudents(s.data||[]);setPayments(p.data||[]);setUnpaid(u.data||[])}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,loading:false}))}};useEffect(()=>{load()},[]);const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async e=>{e.preventDefault();setStatus(s=>({...s,saving:true,error:""}));try{await apiRequest("/school/fee-payments",{method:"POST",body:JSON.stringify({...form,amount:Number(form.amount),paid_for_month:form.paid_for_month||undefined})});setForm({student_id:"",amount:"",payment_method:"cash",paid_for_month:""});await load()}catch(e){setStatus(s=>({...s,error:e.message}))}finally{setStatus(s=>({...s,saving:false}))}};if(status.loading)return <LoadingState/>;return <div className="space-y-5">{unpaid.length>0&&<div className="panel p-4"><h2 className="mb-3 flex items-center gap-2 font-bold"><AlertTriangle className="h-5 w-5 text-red-500"/> Students who haven't paid this month</h2><div className="flex flex-wrap gap-2">{unpaid.map(s=><button key={s.id} onClick={()=>set("student_id",s.id)} className="rounded-full bg-red-50 px-3 py-1.5 text-sm font-semibold text-red-700">{s.name} — {formatMoney(s.monthly_fee)}</button>)}</div></div>}<div className="grid gap-5 xl:grid-cols-[380px_1fr]"><form onSubmit={submit} className="panel h-fit p-4"><h2 className="mb-4 flex items-center gap-2 font-bold"><Wallet className="h-5 w-5 text-blue-600"/> Record a fee payment</h2>{status.error&&<ErrorState message={status.error}/>}<div className="space-y-3"><select className="field" value={form.student_id} onChange={e=>set("student_id",e.target.value)} required><option value="">Choose student…</option>{students.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><input className="field" type="number" min="0.01" step="0.01" placeholder="Amount" value={form.amount} onChange={e=>set("amount",e.target.value)} required/><select className="field" value={form.payment_method} onChange={e=>set("payment_method",e.target.value)}><option value="cash">Cash</option><option value="bank">Bank</option><option value="mobile_money">Mobile money</option></select><input className="field" type="date" value={form.paid_for_month} onChange={e=>set("paid_for_month",e.target.value)}/></div><button className="btn-primary mt-4 w-full" disabled={status.saving}>{status.saving?"Saving…":"Record payment"}</button></form><div className="panel overflow-hidden"><div className="border-b border-slate-100 p-4"><h2 className="font-bold">Payment history</h2></div>{payments.length===0?<div className="p-4"><EmptyState title="No payments yet" description="Record a fee payment to see it here."/></div>:<div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase"><tr><th className="px-4 py-3">Student</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Paid</th><th className="px-4 py-3 text-right">Amount</th></tr></thead><tbody className="divide-y divide-slate-100">{payments.map(p=><tr key={p.id}><td className="px-4 py-3 font-semibold">{p.school_students?.name||"-"}</td><td className="px-4 py-3 capitalize">{p.payment_method}</td><td className="px-4 py-3">{p.paid_at?new Date(p.paid_at).toLocaleDateString():"-"}</td><td className="px-4 py-3 text-right font-bold">{formatMoney(p.amount)}</td></tr>)}</tbody></table></div>}</div></div></div>}
