// Sahel feature switches.
// Hospital is intentionally paused while the module is rebuilt.
// Keep the hospital source files in place so the system can be restored later.
export const HOSPITAL_SYSTEM_ENABLED = false;
