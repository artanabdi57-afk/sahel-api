{
  "name": "sahel-web",
  "version": "1.0.3",
  "description": "Sahel shop management desktop app",
  "author": "Sahel",
  "private": true,
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "desktop:dev": "concurrently \"npm run dev\" \"wait-on http://localhost:5173 && electron .\"",
    "desktop:pack": "npm run build && node electron/prepare-api.cjs && electron-builder --win portable",
    "desktop:build": "npm run build && node electron/prepare-api.cjs && electron-builder --win nsis",
    "desktop:folder": "npm run build && node electron/prepare-api.cjs && electron-packager . Sahel --platform=win32 --arch=x64 --out=release --overwrite --asar=false --icon=assets/sahel-icon.ico --extra-resource=desktop-api --ignore=\"^/node_modules\" --ignore=\"^/src\" --ignore=\"^/public\" --ignore=\"^/android\" --ignore=\"^/release\" --ignore=\"^/release-logo\" --ignore=\"^/frontend.log\" --ignore=\"^/.env\" --ignore=\"^/.env.txt\" --ignore=\"^/desktop-api\"",
    "desktop:installer": "npm run desktop:folder && node electron/create-installer.cjs",
    "mobile:sync": "npm run build && cap sync android",
    "mobile:open": "cap open android",
    "mobile:apk": "npm run mobile:sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@supabase/supabase-js": "^2.108.1",
    "@vitejs/plugin-react": "^4.3.4",
    "html2pdf.js": "^0.10.2",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "recharts": "^2.13.3",
    "vite": "^5.4.11",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.0",
    "@electron/packager": "^20.0.1",
    "autoprefixer": "^10.4.20",
    "concurrently": "^10.0.3",
    "electron": "^42.4.0",
